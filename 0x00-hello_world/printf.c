#include <stdio.h>
/**
 * Entry Point - Main
 * prints exactly with proper grammar, 
 * but the outcome is a piece of art
 * Followed by a new line
 * Return 0 (Succesful)
 */
int main(void)
{
	printf("with proper grammer, but the output is a piece of art: \n");
	return(0);
}
